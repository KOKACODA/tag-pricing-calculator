# 海洋吊牌报价系统 v3.2 变更说明

> 日期：2026-08-03
> 文件：`tag-pricing-calculator-v3.2.html` / `index.html`

## 一、本次变更

### 1.1 个人主页整合「数据管理」与「更多」

为减少导航层级、聚焦高频操作，把原本独立的两个页面（数据管理 / 更多）合并到「个人主页」中：

- **移除顶部导航栏的「数据管理」和「更多」按钮**。
  - 导航栏从 5 个标签精简为 3 个：智能报价计算器 / 报价表查询 / 个人主页。
- **「数据管理」卡片插入到「客户建议价格 · 毛利系数」与「公司信息与报价单设置」之间**。
  - 保留原有 3 个子标签：本地快照 / 报价历史 / 云同步。
  - 价格配置导入/导出、Excel 报价表导入/导出、吊绳报价导入/导出、JSON 配置导入/导出、本地备份/恢复、本地快照列表全部保留。
- **「更多」卡片（扩展功能中心 + 关于与版本）放置在「个人主页」最底部**。
  - 客户管理 / 订单管理 / 统计报表 / 价格趋势 / 打印报价单 / 系统设置 6 个占位功能保留。
  - 关于与版本中的「当前版本」同步刷新为 v3.2。
- **删除独立的 `page-data` 与 `page-more` 两个 `<section>`**。
  - 切换逻辑调整：进入「个人主页」时同时刷新客户等级、个人资料、本地快照、报价历史。

### 1.2 个人主页当前顺序

1. 顶部头像与角色卡片
2. 客户建议价格 · 毛利系数
3. **数据管理**（本地快照 / 报价历史 / 云同步）
4. 公司信息与报价单设置
5. **更多**（扩展功能中心）
6. 关于与版本

## 二、行为说明

- 进入「个人主页」时，会同时执行 `renderLevelSettings()` / `loadProfileToUI()` / `renderSnapshots()` / `renderHistory()`，确保所有子模块内容是新的。
- 所有「数据管理」与「更多」原有功能 ID（如 `exportExcelBtn`、`exportLocalBackupBtn`、`addHistoryPlaceholderBtn` 等）保持不变，按钮事件无需重新绑定。
- 顶部导航栏现在只显示当前页面对应的激活态，UI 视觉更聚焦。
- 「关于与版本」中显示版本号已同步为 v3.2。
- 导出 JSON / 本地备份中的 `version` 字段由 `3.1` 升级为 `3.2`，旧版导出的 3.1 备份依旧可被新版识别（通过 `kind` 校验）。

## 三、兼容性

- 旧版（v3.1）导出的本地备份文件可被 v3.2 正常恢复。
- 旧版（v3.1）导出的完整配置 JSON 文件同样可被 v3.2 正常导入。
- 旧版 `localStorage` 数据继续有效，无需额外迁移。

## 四、文件清单

- 新增：`tag-pricing-calculator-v3.2.html`（本次主版本）
- 同步：`index.html`（部署入口，与 v3.2 内容一致）
- 旧版保留：`tag-pricing-calculator-v3.1.html`（可继续作为历史版本使用）
- 变更说明：`CHANGELOG-v3.2.md`（本文档）
